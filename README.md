# cynaptics2026
I have built a scaled-down, decoder-only Transformer (GPT-2 style) from scratch using PyTorch and trained it on the Tiny Shakespeare dataset to perform text autocompletion, essentially functioning as a highly complex autocomplete model.
